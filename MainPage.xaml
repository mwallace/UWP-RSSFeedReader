<Page
    x:Class="UWPTestApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPTestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView  ItemsSource="{x:Bind Customers}" IsItemClickEnabled="True" ItemClick="ClickItemList"
            HorizontalAlignment="Center"
            VerticalAlignment="Center" Margin="36,92,411,433" Width="993" Height="435">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Customer">
                    <TextBlock Text="{x:Bind Name, Mode=OneWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock HorizontalAlignment="Left" Margin="36,10,0,0" TextWrapping="Wrap" Text="Please Enter RSS Feed URL:" VerticalAlignment="Top" Width="279"/>
        <TextBox x:Name="RSSFeedURL" HorizontalAlignment="Left" Margin="36,35,0,0" TextWrapping="Wrap" Text="https://news.xbox.com/en-us/feed/stories/" VerticalAlignment="Top" Width="279" TextChanged="RSSFeedURL_TextChanged"/>
        <WebView x:Name="WebView_0" HorizontalAlignment="Left" Height="418" VerticalAlignment="Top" Width="993" Margin="36,532,0,0"/>
    </Grid>
</Page>
